services:
  app:
    build: .
    image: ai-shopkeeper:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./bizinsight.db:/app/bizinsight.db
      - ./faiss_store:/app/faiss_store
      - ./media:/app/media
      - ./logs:/app/logs
    restart: unless-stopped
